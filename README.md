# Esp8266-Probe-Sniffer
An app for esp8266 to sniff probe requests and upload to Wigle.

# Flash .bin using Arduino, Esptool, etc.

# Has web interface at 192.268.4.1 after signing into AP.

# Has direct upload to Wigle, but must input API token into firmware and recompiling the .bin with user data.

# Directions in file notes.
